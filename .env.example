# The deployed Modal API URL (from `modal deploy`). Required in Vercel's
# env vars — the deployed site must call this, never localhost.
NEXT_PUBLIC_API_URL=https://emilysmith291--wnba-comp-finder-web.modal.run
